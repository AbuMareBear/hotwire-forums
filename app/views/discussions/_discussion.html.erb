<div id="<%= dom_id discussion %>" class="mb-2">
  <h2><%= discussion.name %></h2>
  <p>Pinned: <%= discussion.pinned %> | Closed: <%= discussion.closed %></p>
  <div>
    <%= link_to "Edit", edit_discussion_path(discussion) %> |
    <%= link_to "Delete", discussion, method: :delete, data: { confirm: "Are your sure? This cannot be undone." } %>
  </div>
</div>
